In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR27 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os, tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, playArea, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR70 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, tag]
and a follow-up test case is constructed by removing the following parameters [feature, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR273 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR243 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, os, price, tag]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
and a follow-up test case is constructed by removing the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR112 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR67 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [feature, price, tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR300 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [language, playArea, tag, vrSupport]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR222 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [os, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [feature, playArea, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR9 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, language, price]
and a follow-up test case is constructed by removing the following parameters [term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR298 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [input, playArea, tag]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR152 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [headsets, input, vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR169 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, os, playArea, price, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR366 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [feature, language]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR63 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by adding the following parameters [os, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR365 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [feature, language, os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR277 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language, playArea]
and a follow-up test case is constructed by adding the following parameters [term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR257 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, selectedTypes, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [os, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR193 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [input, tag, term]
and a follow-up test case is constructed by removing the following parameters [playArea, price]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR285 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [os, price]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR199 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [headsets, language, numberOfPlayers, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR237 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, playArea, price, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR154 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, os]
and a follow-up test case is constructed by adding the following parameters [playArea, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR37 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, language, price, vrSupport]
and a follow-up test case is constructed by adding the following parameters [term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR333 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [input]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, playArea, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR283 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [headsets, os, price, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR370 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, os, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR164 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, price, selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [headsets, playArea, vrSupport]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR106 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [playArea, tag, term]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [headsets, input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR181 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR194 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os, term]
and a follow-up test case is constructed by removing the following parameters [input, playArea, price, tag]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR0 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, price, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR50 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, language, numberOfPlayers, playArea]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [input, tag]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR173 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, playArea, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, price, selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR389 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers, playArea, price, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR295 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [language, playArea]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR16 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, os, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR149 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, term]
and a follow-up test case is constructed by removing the following parameters [price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [language]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR351 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [headsets, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [playArea, price, tag, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR52 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, language, playArea]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, selectedTypes, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR343 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, language, os, price, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR143 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, price, selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR320 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR321 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, price]
and a follow-up test case is constructed by adding the following parameters [headsets, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR203 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, language, vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR373 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, os, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR114 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, os, price, term]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR195 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [os, playArea, price]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR13 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [os, tag]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR350 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:lowestPrice, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR251 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, term]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR66 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes:games, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:downloableContent]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR278 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR99 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [input, playArea, term]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR180 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [input, language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR184 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, os, playArea, price, tag, term]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR113 (DISJOINT_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes:games, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:downloableContent]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:videos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:mods]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR303 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes:downloableContent, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:games]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR327 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [feature, input, numberOfPlayers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR177 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [feature, language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR337 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [language, os, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR191 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, os, playArea, price, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR259 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, price, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR40 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [headsets, language, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [feature, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR299 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [language, playArea, tag]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR374 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [os, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR212 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, input, language, numberOfPlayers, tag, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR361 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, playArea, selectedTypes, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR108 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [feature, tag, term]
and a follow-up test case is constructed by adding the following parameters [input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR314 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, os, playArea, selectedTypes, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR375 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [language, os, term]
and a follow-up test case is constructed by removing the following parameters [feature, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR153 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR105 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, input, tag, term]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR155 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, playArea]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR161 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, playArea, vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, os]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR272 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [language, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, input, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR301 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [language, vrSupport]
and a follow-up test case is constructed by adding the following parameters [input, playArea, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR75 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [selectedTypes, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, playArea]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR6 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [language, price, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, term]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR91 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [os, price]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
and a follow-up test case is constructed by removing the following parameters [language]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR69 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR376 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR304 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, playArea, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language, os, selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR315 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [os, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, language, playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR262 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os, selectedTypes, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR136 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR138 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, price, tag, term]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR185 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [input, price, selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR122 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, os, term]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR82 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language, os, playArea, vrSupport]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR306 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, os, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR357 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, playArea, price, selectedTypes, tag, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR107 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, input, playArea, tag]
and a follow-up test case is constructed by adding the following parameters [term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR276 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [headsets, language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR293 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [input, language, playArea, tag]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR218 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, input, language, numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [tag, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR390 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [headsets, input, playArea, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR190 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [input]
and a follow-up test case is constructed by removing the following parameters [os, price]
and a follow-up test case is constructed by removing the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR296 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [input, language, playArea, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR216 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [language, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, input, term]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR334 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, input, numberOfPlayers, playArea, vrSupport]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR232 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, os, playArea, price, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR346 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [os, price]
and a follow-up test case is constructed by adding the following parameters [headsets, language, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR354 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, playArea, price, selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [input, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR38 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR65 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR186 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, playArea, price, selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR59 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, input, language, numberOfPlayers, playArea, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR228 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [feature, playArea, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR54 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, language, numberOfPlayers, playArea, selectedTypes, tag, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR265 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR213 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [language, numberOfPlayers, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, input, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR227 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [os, playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR160 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, os, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, input]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR47 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR163 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes:downloableContent, feature, language]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:games]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:videos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:mods]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR233 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input]
and a follow-up test case is constructed by removing the following parameters [playArea, price, tag]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, os, selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR64 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR90 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os]
and a follow-up test case is constructed by removing the following parameters [language, price, selectedTypes, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR242 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, input, playArea, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [feature, os, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR221 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [feature, os, playArea, price, selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR377 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language, os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR341 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [language, os, price]
and a follow-up test case is constructed by adding the following parameters [headsets, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR235 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, input, os, playArea, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [headsets, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR359 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [headsets, input, playArea, price, selectedTypes, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR151 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, os, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR42 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [headsets, price]
and a follow-up test case is constructed by adding the following parameters [feature, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR254 (DISJOINT_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes:games, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR53 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [headsets, input, tag, term]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [language, playArea]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR79 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, os, playArea, selectedTypes, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR312 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, os, playArea, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR45 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, language, price, term, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR36 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, language, price, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR110 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, playArea]
and a follow-up test case is constructed by adding the following parameters [feature, input, tag, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR175 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, os]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [playArea, tag, term]
and a follow-up test case is constructed by removing the following parameters [price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR146 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [price, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR380 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, price, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [input, playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR142 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, price, selectedTypes, tag, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR76 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, language, os, playArea, selectedTypes, vrSupport]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR269 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [language, playArea]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [headsets, input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR68 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, price, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR100 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, input, playArea, term]
and a follow-up test case is constructed by adding the following parameters [tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR248 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR234 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [input, os, playArea, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR297 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [input, playArea, tag, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR368 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:lowestPrice, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:userReviews]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR189 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, os, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [playArea, price, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR11 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [input, playArea]
and a follow-up test case is constructed by adding the following parameters [os, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR253 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [sortBy:lowestPrice, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR384 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, playArea, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [input, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR311 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, os, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language, playArea, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR159 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, os]
and a follow-up test case is constructed by adding the following parameters [input, playArea]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR26 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [os, tag]
and a follow-up test case is constructed by removing the following parameters [input]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
and a follow-up test case is constructed by removing the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR391 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR166 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, os, playArea, price, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR187 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, price]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [playArea, term]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR280 (DISJOINT_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes:games, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:downloableContent]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:videos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:mods]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR378 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [os, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language, term]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR56 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, language, numberOfPlayers, playArea, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR140 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [feature, language, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR225 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [feature, os]
and a follow-up test case is constructed by adding the following parameters [playArea, price, selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR55 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [headsets, selectedTypes, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, input, language, playArea, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR87 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [sortBy:relevance, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR167 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, os, playArea, price, selectedTypes, tag, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR25 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, playArea, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [os, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR104 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [feature, headsets]
and a follow-up test case is constructed by adding the following parameters [playArea, tag, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR34 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, playArea, tag]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR336 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, playArea, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR229 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [feature, os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR72 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, price, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR124 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [input]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [headsets, playArea, price]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR80 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, language, os, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [input, playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR270 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR310 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, os, playArea, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR317 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers, price, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR294 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [input, language, tag, vrSupport]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR309 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [playArea, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [language, os]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR61 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, numberOfPlayers, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, input, playArea, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR214 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [headsets, input, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR211 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, language, numberOfPlayers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [input, term]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR33 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, os, playArea, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR58 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, language, numberOfPlayers, selectedTypes, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [input, playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR367 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR51 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, input, numberOfPlayers, playArea, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [headsets, language, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR81 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [input, language, playArea, selectedTypes, term]
and a follow-up test case is constructed by adding the following parameters [headsets, vrSupport]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR144 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [feature, price, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR209 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, input, language, numberOfPlayers, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR363 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [feature, os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR239 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, os, playArea, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR201 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [headsets, language, vrSupport]
and a follow-up test case is constructed by adding the following parameters [feature, numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR134 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [language, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR348 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, os, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR71 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [price, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR48 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [sortBy:relevance, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:userReviews]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR381 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [input, playArea, price, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [headsets, tag]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR39 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, term]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, price, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR73 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [feature, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR224 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [feature, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [os, playArea, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR2 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, price, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR290 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [headsets, price]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR101 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [feature, input, playArea, tag, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR349 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR249 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR176 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, input]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR358 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [playArea, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, price, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR217 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, input, language, numberOfPlayers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR43 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR330 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, playArea]
and a follow-up test case is constructed by removing the following parameters [input, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR372 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, term]
and a follow-up test case is constructed by removing the following parameters [os, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR28 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, os, playArea, tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR5 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, language, vrSupport]
and a follow-up test case is constructed by removing the following parameters [price, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR170 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, os, playArea, price, selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR96 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [os, price]
and a follow-up test case is constructed by removing the following parameters [language, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR356 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [input, term]
and a follow-up test case is constructed by removing the following parameters [headsets, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, playArea, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR62 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [input, tag]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, language, numberOfPlayers, playArea, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR196 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, os, playArea, price, term]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR174 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, os, playArea]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR23 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, playArea, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR219 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, term]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, tag]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [input]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR313 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [language, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, os, playArea, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR183 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes:downloableContent, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:games]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:videos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:mods]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR325 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:relevance, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR208 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes:games, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:downloableContent]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR344 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, os, price, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR247 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR353 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [playArea, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR324 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR326 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes:downloableContent, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:games]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR287 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, os, price, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR261 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, price, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR200 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [feature, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR307 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, playArea, term]
and a follow-up test case is constructed by removing the following parameters [language, vrSupport]
and a follow-up test case is constructed by removing the following parameters [os, selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR383 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, playArea, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR292 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [feature, os, price]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR119 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, input, os, price, term]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR252 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR281 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [os, term]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, numberOfPlayers, price, selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR323 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, tag]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR162 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:userReviews]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR264 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR258 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, price, tag]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR387 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, playArea, price, tag]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR331 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, playArea, term]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR57 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, language, numberOfPlayers, playArea, selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR83 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language, os, selectedTypes, vrSupport]
and a follow-up test case is constructed by adding the following parameters [playArea, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR250 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR332 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, input, numberOfPlayers, playArea, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR245 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [term, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR21 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR302 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:userReviews]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR46 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [headsets, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR282 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, numberOfPlayers, os, price, selectedTypes, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR308 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [language, os, playArea, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR89 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [language, os, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [price, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR78 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [input, language, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets, os, playArea]
and a follow-up test case is constructed by adding the following parameters [selectedTypes, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR379 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, os]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR3 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, language, price, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR168 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, playArea, tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, price, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR86 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR236 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [headsets, input, os, playArea, price]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR139 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [language, price, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR118 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, numberOfPlayers, os, playArea, price, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR93 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [language, numberOfPlayers, os, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR188 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, price]
and a follow-up test case is constructed by removing the following parameters [playArea, term]
and a follow-up test case is constructed by removing the following parameters [os, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR288 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, os, price, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR328 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, playArea, vrSupport]
and a follow-up test case is constructed by removing the following parameters [input]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR18 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, os, playArea, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR206 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR345 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, language, os, tag]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR352 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [playArea, price]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR4 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [headsets, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, term]
and a follow-up test case is constructed by removing the following parameters [language, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR204 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, language, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR130 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, vrSupport]
and a follow-up test case is constructed by adding the following parameters [selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR362 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [language, os]
and a follow-up test case is constructed by adding the following parameters [feature]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR126 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, price, term]
and a follow-up test case is constructed by removing the following parameters [headsets, os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR241 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, os, playArea, price, tag]
and a follow-up test case is constructed by removing the following parameters [headsets, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR19 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, os, tag]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR150 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [input, playArea]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR289 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [headsets, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [feature, term]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR318 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR202 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, language, numberOfPlayers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR84 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, language, os, selectedTypes, term]
and a follow-up test case is constructed by adding the following parameters [input, playArea]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR102 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [input, playArea, tag, term]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR223 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [feature, price]
and a follow-up test case is constructed by adding the following parameters [os, playArea]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR131 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [language, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR44 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, language, price, term]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR115 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [feature, term]
and a follow-up test case is constructed by removing the following parameters [headsets, os, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR322 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [price, tag]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR123 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, os, playArea, price, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR246 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, term, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR198 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, language, numberOfPlayers, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR182 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:lowestPrice, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR226 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [feature, playArea, price, selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR255 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [feature, os, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [tag, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR388 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers, playArea, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR109 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, playArea]
and a follow-up test case is constructed by adding the following parameters [tag, term]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR329 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, playArea]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [input, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR94 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [language, os, price, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR29 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, os, playArea, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR14 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [os, playArea, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR157 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers, os, playArea, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR355 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [price, term]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, playArea, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR291 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [feature, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR12 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, os, playArea, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR238 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, input, os, playArea, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR1 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, price, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR156 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, os, playArea, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR41 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets]
and a follow-up test case is constructed by adding the following parameters [price, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR244 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, numberOfPlayers, term, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR117 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, term]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, numberOfPlayers, price]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR22 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:lowestPrice, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR385 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, price]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR145 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, term]
and a follow-up test case is constructed by removing the following parameters [price, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR274 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [input, language, playArea, term]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR95 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [language, numberOfPlayers, os, price, selectedTypes, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR275 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [input, language]
and a follow-up test case is constructed by adding the following parameters [headsets, playArea]
and a follow-up test case is constructed by adding the following parameters [term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR148 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [language, price, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR97 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [language, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR197 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR85 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, input, os, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language, playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR171 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, playArea, tag]
and a follow-up test case is constructed by removing the following parameters [headsets, os, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR74 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [input, language, playArea, term]
and a follow-up test case is constructed by adding the following parameters [headsets, os, selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR335 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, playArea, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR364 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR263 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os, price, selectedTypes, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR165 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os, playArea, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, price]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, selectedTypes, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR92 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, price, vrSupport]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
and a follow-up test case is constructed by removing the following parameters [language]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR147 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [language, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR31 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, playArea]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR120 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os, playArea, price, term]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR103 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, input, playArea, tag, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR88 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os]
and a follow-up test case is constructed by removing the following parameters [language, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR24 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, playArea, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR20 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, playArea, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR15 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [input, os, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR231 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:relevance, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR30 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [playArea, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [input, os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR268 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [input, term]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR179 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR316 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, price, tag]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR116 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, price]
and a follow-up test case is constructed by removing the following parameters [feature, os, playArea, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR286 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [feature, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR256 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, price, selectedTypes, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR8 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR371 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, os, vrSupport]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR7 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, price, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR230 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR178 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, input, language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR137 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:lowestPrice, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR127 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, tag, vrSupport]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR220 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [feature, playArea]
and a follow-up test case is constructed by adding the following parameters [os, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR215 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, language, numberOfPlayers, tag, term]
and a follow-up test case is constructed by removing the following parameters [input, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR340 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [headsets, language, os, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR132 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, selectedTypes, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR207 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:relevance, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR347 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [language, os, tag]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR210 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, input, tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [language, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR125 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, playArea, price]
and a follow-up test case is constructed by removing the following parameters [feature, input, os, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR369 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes:downloableContent, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:games]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR260 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature, os, price, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR60 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, language, numberOfPlayers, term]
and a follow-up test case is constructed by removing the following parameters [headsets, input, playArea, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR49 (DISJOINT_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes:downloableContent, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:games]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR133 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [selectedTypes, tag, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR10 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [os, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR121 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, price]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, os, playArea, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR382 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, playArea, price]
and a follow-up test case is constructed by adding the following parameters [input, tag]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR172 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, os, playArea, price, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR266 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os, price, vrSupport]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR284 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, price, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [feature, os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR111 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, tag]
and a follow-up test case is constructed by adding the following parameters [feature, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR267 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language, playArea, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR240 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, input, os, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [playArea, price, tag]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR386 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [price, tag]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR271 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [input, language, term]
and a follow-up test case is constructed by adding the following parameters [headsets, playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR305 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, playArea, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language, os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR129 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR205 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, numberOfPlayers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [feature]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR339 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, os, price, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR32 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, os]
and a follow-up test case is constructed by removing the following parameters [playArea, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR279 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [sortBy:lowestPrice, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR141 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR35 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, playArea]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR319 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, tag]
and a follow-up test case is constructed by adding the following parameters [input, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR128 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR98 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, specialOffers, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [language, os, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, price]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR17 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, os, playArea, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR77 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language, os, playArea, selectedTypes, term, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR342 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [os, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [headsets, language, price, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR360 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [price, tag]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [headsets, input, playArea, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR338 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, price]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR158 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, os, playArea, vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR135 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [tag, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR192 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [playArea, price]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

