In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR405 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, language, os, specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR105 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [language, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [os, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR104 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [price, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language, numberOfPlayers, os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR272 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR385 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, price, selectedTypes, specialOffers, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [input, term]
and a follow-up test case is constructed by removing the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR42 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, price, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR248 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [feature, os, selectedTypes, specialOffers]
and a follow-up test case is constructed by adding the following parameters [playArea, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR331 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [language, os, playArea, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
and a follow-up test case is constructed by removing the following parameters [feature, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR245 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [playArea, price, selectedTypes, specialOffers]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR144 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR41 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [headsets, term]
and a follow-up test case is constructed by adding the following parameters [feature, language, vrSupport]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR0 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [headsets, language, vrSupport]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR80 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, tag]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR94 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language, selectedTypes, specialOffers]
and a follow-up test case is constructed by adding the following parameters [os, term]
and a follow-up test case is constructed by adding the following parameters [playArea, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR176 (EQUIVALENCE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
then the source and the follow-up output(s) should have the same items in the same order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR121 (EQUIVALENCE_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
then the source and the follow-up output(s) should have the same items in the same order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR316 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [input, language, playArea, vrSupport]
and a follow-up test case is constructed by adding the following parameters [tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR212 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, language, numberOfPlayers, specialOffers, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR59 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, input, language, playArea, specialOffers, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR370 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, price, selectedTypes, specialOffers, tag]
and a follow-up test case is constructed by adding the following parameters [language, os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR12 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [specialOffers, tag]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, os, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR123 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, input, numberOfPlayers, os, playArea, price, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR357 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [playArea, term]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR231 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [language, term]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR86 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language, os, playArea, selectedTypes, specialOffers, term, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR44 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, language]
and a follow-up test case is constructed by adding the following parameters [price, term, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR277 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [feature, price, selectedTypes, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR379 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, price, selectedTypes, specialOffers, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR333 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, os, playArea, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR305 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [headsets, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os, specialOffers]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR68 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR51 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [sortBy:relevance, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR381 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [playArea, price, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, tag]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR410 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, os, specialOffers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR92 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [input, os, playArea, specialOffers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language, selectedTypes, term]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR291 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [input, language, playArea]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR54 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, language, numberOfPlayers, playArea, selectedTypes, specialOffers, tag, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR384 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [headsets, playArea, price, specialOffers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR31 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, playArea, tag]
and a follow-up test case is constructed by removing the following parameters [os, selectedTypes, specialOffers]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR389 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, price, selectedTypes, specialOffers, tag, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR188 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, playArea, selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [headsets, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR246 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [feature, playArea, price, selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR165 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, playArea, vrSupport]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR274 (DISJOINT_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes:games, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:downloableContent]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:videos]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR314 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [input, playArea, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR296 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [input, language, playArea, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR177 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, price, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [os, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR32 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, playArea, selectedTypes, specialOffers]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR375 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR221 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [feature, specialOffers]
and a follow-up test case is constructed by adding the following parameters [language, vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR193 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by adding the following parameters [input, language]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR118 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, playArea, specialOffers, tag]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR38 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, vrSupport]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR292 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [language, playArea, term]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR362 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, input, playArea]
and a follow-up test case is constructed by removing the following parameters [term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR88 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [playArea, specialOffers]
and a follow-up test case is constructed by adding the following parameters [headsets, os, term]
and a follow-up test case is constructed by adding the following parameters [input, selectedTypes, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR134 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, specialOffers]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, playArea]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [os, price, term]
and a follow-up test case is constructed by removing the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR196 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR30 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, os, playArea, selectedTypes, specialOffers, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR306 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, selectedTypes, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR220 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [headsets, language, specialOffers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [feature, numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR207 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, os, playArea, price, tag, term]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR393 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR386 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [playArea, term]
and a follow-up test case is constructed by removing the following parameters [headsets, input, price, selectedTypes, specialOffers]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR219 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [headsets, vrSupport]
and a follow-up test case is constructed by adding the following parameters [feature, language, numberOfPlayers, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR234 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, language, numberOfPlayers, tag]
and a follow-up test case is constructed by removing the following parameters [input, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR182 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [os, playArea, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [headsets, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR97 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [language, price, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR184 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, playArea, tag]
and a follow-up test case is constructed by removing the following parameters [os, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
and a follow-up test case is constructed by removing the following parameters [headsets, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR308 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, numberOfPlayers, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [os, specialOffers, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR168 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [input, os]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [playArea, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR244 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [feature, os, price, specialOffers]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR356 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, input, numberOfPlayers, term]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR327 (EQUIVALENCE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
then the source and the follow-up output(s) should have the same items in the same order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR90 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [input, language, term]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [headsets, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [specialOffers, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR250 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:relevance, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:userReviews]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR240 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [feature, os, playArea, selectedTypes, specialOffers]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR50 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR189 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, language, specialOffers]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR259 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, specialOffers]
and a follow-up test case is constructed by removing the following parameters [price, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [os, playArea]
and a follow-up test case is constructed by removing the following parameters [input]
and a follow-up test case is constructed by removing the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR192 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by adding the following parameters [input, language]
and a follow-up test case is constructed by adding the following parameters [feature, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR79 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [feature, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR239 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, tag, term]
and a follow-up test case is constructed by removing the following parameters [input, language, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR401 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [os, term]
and a follow-up test case is constructed by removing the following parameters [language]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR255 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, input, os, playArea, price, selectedTypes, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR233 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [input, language, tag, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR344 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers, price]
and a follow-up test case is constructed by adding the following parameters [tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR330 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [language, os, selectedTypes, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [feature, playArea, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR329 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, os, playArea, selectedTypes, specialOffers, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR404 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [language]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [feature, term]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR348 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [input, price]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR224 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [headsets, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [feature, numberOfPlayers, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR186 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, playArea, price, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR213 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, language, specialOffers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR187 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [os, playArea, price, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR70 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes:games, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:downloableContent]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:videos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:mods]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR350 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:relevance, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR338 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [os, playArea, specialOffers]
and a follow-up test case is constructed by removing the following parameters [feature, language, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR282 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, os, specialOffers]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, tag]
and a follow-up test case is constructed by removing the following parameters [price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR423 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, playArea, price, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR290 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language, term]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR387 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, selectedTypes, specialOffers]
and a follow-up test case is constructed by removing the following parameters [playArea, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR106 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [os, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [language, numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR285 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature, os, tag]
and a follow-up test case is constructed by removing the following parameters [price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR37 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, os, playArea, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR280 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os, price, selectedTypes, specialOffers, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR8 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [feature, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR131 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, input]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, term]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [feature, price, specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR136 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, selectedTypes, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR312 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, selectedTypes, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [feature, price]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR93 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [input, playArea, selectedTypes, specialOffers]
and a follow-up test case is constructed by adding the following parameters [language, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR318 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [input, playArea]
and a follow-up test case is constructed by adding the following parameters [tag, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR372 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, os, price, specialOffers]
and a follow-up test case is constructed by adding the following parameters [selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR110 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [feature, input, specialOffers, tag, term]
and a follow-up test case is constructed by adding the following parameters [headsets, playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR369 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, language, os, price, selectedTypes, specialOffers, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR167 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, os, playArea]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR335 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [language, os, playArea, selectedTypes, specialOffers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR113 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, tag, term]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR119 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR60 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, input, language, playArea]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, selectedTypes, specialOffers]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR264 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, numberOfPlayers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR162 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, os, playArea, vrSupport]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR229 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, input, language, numberOfPlayers, tag, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR336 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [language, specialOffers]
and a follow-up test case is constructed by removing the following parameters [feature, os, playArea, selectedTypes, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR419 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers, playArea, price, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR289 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language, playArea]
and a follow-up test case is constructed by adding the following parameters [term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR340 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, tag]
and a follow-up test case is constructed by adding the following parameters [headsets, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR210 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, os, playArea, price, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR249 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR307 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, numberOfPlayers, os, price, selectedTypes, specialOffers, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR197 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:lowestPrice, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR18 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [os, specialOffers]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [input, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR153 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, price, specialOffers, tag, term]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR421 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [input, playArea, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, price, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR91 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, language, os, playArea, selectedTypes, specialOffers, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR361 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [input, playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR225 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR395 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [feature, os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR173 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers, os, playArea, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR164 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers, os]
and a follow-up test case is constructed by adding the following parameters [playArea, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR98 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR138 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [tag, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR256 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, os, playArea, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR345 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [input, tag]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR281 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [feature, specialOffers, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR76 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR341 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers, price, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR217 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, numberOfPlayers, specialOffers]
and a follow-up test case is constructed by adding the following parameters [headsets, language, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR62 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [language, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, specialOffers]
and a follow-up test case is constructed by removing the following parameters [input, playArea, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR45 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, term]
and a follow-up test case is constructed by adding the following parameters [language, price]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR400 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, specialOffers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR392 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [feature, os]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR337 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, os, playArea, selectedTypes, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR323 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [tag, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR14 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, os, playArea, selectedTypes, specialOffers, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR181 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, os, playArea, price, selectedTypes, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR57 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [input, language, numberOfPlayers, playArea, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, tag]
and a follow-up test case is constructed by removing the following parameters [feature, selectedTypes, specialOffers, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR178 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, tag]
and a follow-up test case is constructed by removing the following parameters [headsets, os, playArea, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [price, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR183 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, os, playArea, price, selectedTypes, tag, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR258 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, input, price, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [feature, os]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR416 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [price, tag]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR6 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [headsets, language, price, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR332 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [os, playArea, specialOffers]
and a follow-up test case is constructed by removing the following parameters [feature, language, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR352 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [input, playArea, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR260 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, os, playArea]
and a follow-up test case is constructed by removing the following parameters [headsets, input, price, selectedTypes, specialOffers]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR301 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, numberOfPlayers, os, price, selectedTypes, specialOffers]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR275 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [os, specialOffers, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR242 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [feature, os, playArea, price, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR102 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [language, os, price, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR65 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [input, language, playArea, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [feature, headsets]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR67 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR343 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, price, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR271 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR5 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, price, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR420 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, price, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [input, playArea]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR85 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [os, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets, input, selectedTypes, specialOffers]
and a follow-up test case is constructed by adding the following parameters [language, playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR261 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, playArea, specialOffers, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR174 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:userReviews]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR78 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR299 (DISJOINT_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes:games, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR33 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, os, selectedTypes, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR69 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR283 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, tag]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR288 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language, playArea, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR58 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [headsets, language, numberOfPlayers, playArea, selectedTypes, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, input, specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR154 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [feature, language]
and a follow-up test case is constructed by removing the following parameters [price, tag]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR402 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, specialOffers, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language, os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR394 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [feature, language, os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR120 (DISJOINT_EXTEND):
if a source test case is run with the inputs [price, selectedTypes:games, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:downloableContent]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:videos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:mods]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR34 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os, playArea, selectedTypes, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR87 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, input, os, playArea, specialOffers, term]
and a follow-up test case is constructed by adding the following parameters [selectedTypes, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR315 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [input, language, vrSupport]
and a follow-up test case is constructed by adding the following parameters [playArea, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR169 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [input, vrSupport]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR268 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR161 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [input, os, vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR163 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, playArea, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR28 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [os, playArea, specialOffers]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR52 (DISJOINT_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes:downloableContent, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:games]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR115 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR95 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR382 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, price, selectedTypes, specialOffers, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [input, playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR390 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [language, os]
and a follow-up test case is constructed by adding the following parameters [feature]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR172 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, os, playArea, vrSupport]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR185 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [headsets, price]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR273 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [sortBy:lowestPrice, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR230 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, language, term]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, tag]
and a follow-up test case is constructed by removing the following parameters [input, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR287 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os, price, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR209 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, price, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [os, playArea, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR346 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, price, tag]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR349 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR24 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR158 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [feature, language, price, specialOffers, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR295 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR265 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR156 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, price, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
and a follow-up test case is constructed by removing the following parameters [language]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR279 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [os, price, tag]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, specialOffers]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR194 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, input]
and a follow-up test case is constructed by adding the following parameters [language, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR422 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, playArea, tag]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, price, selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR223 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, numberOfPlayers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets, language, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR241 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [feature, os, playArea, price, selectedTypes, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR325 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:userReviews]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR20 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, os, playArea, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR133 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os, price, specialOffers]
and a follow-up test case is constructed by removing the following parameters [headsets, input]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR49 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, language, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR266 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, numberOfPlayers, term, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR321 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [input, tag, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language, playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR16 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, os, playArea]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR160 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [language, price, tag]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR313 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [language, playArea, tag, vrSupport]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR358 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, playArea, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR425 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:userReviews]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR171 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets, os, playArea]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR214 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, language, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [headsets, vrSupport]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR254 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, price, tag]
and a follow-up test case is constructed by removing the following parameters [input, os, playArea, selectedTypes, specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR373 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [os, selectedTypes, specialOffers]
and a follow-up test case is constructed by adding the following parameters [price, tag]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR147 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:lowestPrice, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR23 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, playArea, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, os, specialOffers, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR263 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [language, vrSupport]
and a follow-up test case is constructed by adding the following parameters [term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR251 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [playArea, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [os, price, specialOffers]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR253 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, playArea, selectedTypes, specialOffers]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, os, price, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR247 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [os, playArea, price, selectedTypes, specialOffers]
and a follow-up test case is constructed by adding the following parameters [feature]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR398 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes:downloableContent, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:games]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR303 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [headsets, price, term]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR89 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [input, language, playArea, specialOffers, term]
and a follow-up test case is constructed by adding the following parameters [headsets, os, selectedTypes, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR10 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [language, term]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR218 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, numberOfPlayers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR53 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, language, playArea, selectedTypes, specialOffers, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR29 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [playArea, selectedTypes, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR19 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, os, playArea, selectedTypes, specialOffers]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR148 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [language, specialOffers, tag, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR300 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [headsets, specialOffers]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, price, term]
and a follow-up test case is constructed by removing the following parameters [feature, selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR302 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [headsets, price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, specialOffers, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR359 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, input, numberOfPlayers, playArea, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR216 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, specialOffers]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR211 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, price, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR75 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, tag]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR304 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, numberOfPlayers, os, price, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR152 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [feature, price, selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [language, specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR334 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, playArea, selectedTypes, specialOffers, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language]
and a follow-up test case is constructed by removing the following parameters [os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR109 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [input, playArea, specialOffers, tag, term]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR66 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by adding the following parameters [os, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR326 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes:downloableContent, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:games]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR365 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [language, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [headsets, specialOffers, tag]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR376 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:lowestPrice, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR9 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, language, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR179 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, price, selectedTypes, tag, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR366 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, os, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [price, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR81 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [feature, price]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR252 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, os, playArea, price, selectedTypes, specialOffers, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR47 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [language, price, vrSupport]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [feature]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR140 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [language, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR363 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, input, numberOfPlayers, playArea, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR72 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [feature, tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR190 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, input, language, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR100 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language, os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR328 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by removing the following parameters [language, os, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, specialOffers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR339 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR55 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, language, numberOfPlayers, playArea, vrSupport]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, specialOffers, tag, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR157 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, price, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR116 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [playArea, tag, term]
and a follow-up test case is constructed by adding the following parameters [feature, input, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR141 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR198 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes:downloableContent, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:games]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:videos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:mods]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR353 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [playArea, term]
and a follow-up test case is constructed by removing the following parameters [feature, input, numberOfPlayers, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR360 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, playArea, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR232 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [input, tag, term]
and a follow-up test case is constructed by removing the following parameters [headsets, language, numberOfPlayers, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR317 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [input, tag]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR7 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [headsets, price, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR424 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [playArea, price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR403 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR322 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [language, playArea, vrSupport]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR215 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, language, numberOfPlayers, specialOffers]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR130 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, os, price, specialOffers, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR150 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, price, selectedTypes, specialOffers, tag, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR40 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, price, term]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR199 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [playArea, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [input, os, price, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR56 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [input, language, numberOfPlayers, playArea, selectedTypes, specialOffers, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, headsets]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR108 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [feature, input, specialOffers, term]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR139 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [selectedTypes, vrSupport]
and a follow-up test case is constructed by adding the following parameters [tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR397 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:lowestPrice, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR117 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, input, term]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
and a follow-up test case is constructed by adding the following parameters [playArea, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR364 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, language, os, price, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR96 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [sortBy:relevance, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:userReviews]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR227 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes:games, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:downloableContent]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:videos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:mods]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR166 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, os]
and a follow-up test case is constructed by adding the following parameters [headsets, input, playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR417 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [price, tag]
and a follow-up test case is constructed by adding the following parameters [headsets, input, playArea, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR74 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, price, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR170 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by adding the following parameters [headsets, numberOfPlayers, playArea, vrSupport]
and a follow-up test case is constructed by adding the following parameters [input, os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR414 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, input, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR226 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:relevance, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:lowestPrice]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR347 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [price, tag]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR310 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, os, selectedTypes, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR84 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [playArea, selectedTypes, specialOffers, term]
and a follow-up test case is constructed by adding the following parameters [headsets, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR73 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [feature, price, tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR413 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, playArea, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [headsets, price, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR137 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR71 (EQUIVALENCE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
then the source and the follow-up output(s) should have the same items in the same order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR278 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [os, price, selectedTypes, specialOffers, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR293 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [input, language, playArea, term]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR412 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [headsets, input, numberOfPlayers, playArea, tag]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR180 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by removing the following parameters [playArea, price, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [os, term]
and a follow-up test case is constructed by removing the following parameters [headsets]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR112 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [feature, input, term]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
and a follow-up test case is constructed by adding the following parameters [playArea]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR25 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [term, sortBy:lowestPrice, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR149 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [price, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [feature, language, specialOffers, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR378 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, playArea, price, selectedTypes, specialOffers, tag, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR22 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [specialOffers, tag]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR39 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, language, price, term, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR145 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [language, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR407 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, os, specialOffers, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR267 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR142 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [language, tag, vrSupport]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR297 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR262 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, playArea, price]
and a follow-up test case is constructed by removing the following parameters [os, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR409 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, os, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR236 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, input, language, numberOfPlayers, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR122 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, playArea, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, price]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR309 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, os, specialOffers]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [feature, selectedTypes, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR125 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os, price, term]
and a follow-up test case is constructed by removing the following parameters [headsets, specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR351 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes:downloableContent, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:games]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR243 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by adding the following parameters [os, playArea, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [price, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR237 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, tag]
and a follow-up test case is constructed by removing the following parameters [input, language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR374 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [language, os, price, specialOffers, tag]
and a follow-up test case is constructed by adding the following parameters [headsets, selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR371 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, os, selectedTypes, specialOffers]
and a follow-up test case is constructed by adding the following parameters [language, price, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR269 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, term]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR146 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR46 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, language, vrSupport]
and a follow-up test case is constructed by adding the following parameters [price]
and a follow-up test case is constructed by adding the following parameters [term]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR383 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [input, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [headsets, playArea, price, specialOffers, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR35 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, os, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
and a follow-up test case is constructed by removing the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR399 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, os, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR408 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [language, os, specialOffers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR380 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [headsets, numberOfPlayers, price, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [input, playArea, selectedTypes, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR36 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, os, playArea, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR354 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, playArea, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR391 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR191 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, specialOffers]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [language]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR377 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, playArea, price, selectedTypes, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR206 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [input, playArea, price, tag, term]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR418 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, selectedTypes]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, playArea]
and a follow-up test case is constructed by adding the following parameters [input, price]
and a follow-up test case is constructed by adding the following parameters [tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR367 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [price, specialOffers, tag]
and a follow-up test case is constructed by adding the following parameters [headsets, language, os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR202 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, price, selectedTypes, tag]
and a follow-up test case is constructed by removing the following parameters [os, playArea]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR132 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, os, playArea, specialOffers]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, price, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR276 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, price, selectedTypes, specialOffers, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR77 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, price]
and a follow-up test case is constructed by removing the following parameters [feature]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR127 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, input]
and a follow-up test case is constructed by removing the following parameters [os, playArea, price]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, term]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR101 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, price, selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR13 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, playArea, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, os, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR43 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, language, price]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR21 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, tag]
and a follow-up test case is constructed by adding the following parameters [playArea, selectedTypes, specialOffers]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR159 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, price, specialOffers]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [language]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR2 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, language, price, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR270 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers]
and a follow-up test case is constructed by adding the following parameters [language, term]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR17 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, playArea, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR294 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by adding the following parameters [input, language, term]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR82 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [input]
and a follow-up test case is constructed by adding the following parameters [os]
and a follow-up test case is constructed by adding the following parameters [language, playArea, specialOffers, term]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets, selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR175 (DISJOINT_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes:downloableContent, feature, language]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:games]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:software]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:demos]
and a follow-up test case is constructed by changing the value of the following parameters [selectedTypes:soundtracks]
then the source and the follow-up output(s) should be disjoint.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR129 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, input, price, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os, playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR27 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [playArea, specialOffers]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, os, selectedTypes, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR320 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [input, playArea, tag]
and a follow-up test case is constructed by adding the following parameters [language, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR201 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, tag]
and a follow-up test case is constructed by removing the following parameters [os, playArea, price, selectedTypes, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR204 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, term]
and a follow-up test case is constructed by removing the following parameters [input, os, playArea, tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR205 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [playArea, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [input, term]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR1 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, price, vrSupport]
and a follow-up test case is constructed by removing the following parameters [headsets, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR319 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [language, tag]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
and a follow-up test case is constructed by adding the following parameters [input, playArea]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR415 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, playArea]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, selectedTypes, tag]
and a follow-up test case is constructed by adding the following parameters [price]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR135 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [tag, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language, selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR99 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [language, numberOfPlayers, os, price, selectedTypes, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR342 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, specialOffers, selectedTypes, feature, vrSupport, playArea, os, language]
and a follow-up test case is constructed by adding the following parameters [tag]
and a follow-up test case is constructed by adding the following parameters [headsets]
and a follow-up test case is constructed by adding the following parameters [numberOfPlayers, price]
and a follow-up test case is constructed by adding the following parameters [input]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR228 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, language, numberOfPlayers, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [input]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR128 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, numberOfPlayers, os, specialOffers]
and a follow-up test case is constructed by removing the following parameters [playArea, price, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR124 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, price]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, os, specialOffers]
and a follow-up test case is constructed by removing the following parameters [playArea]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR411 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [feature, os, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [language]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR114 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [feature, headsets, input, playArea, specialOffers, tag, term]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR396 (SHUFFLING_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [sortBy]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR3 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, language, price]
and a follow-up test case is constructed by removing the following parameters [term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR203 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, os, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [playArea, price, tag, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR63 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, language, numberOfPlayers, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [specialOffers]
and a follow-up test case is constructed by removing the following parameters [playArea]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR208 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [os, price, tag, term]
and a follow-up test case is constructed by removing the following parameters [input, playArea]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR426 (EQUIVALENCE_EXTEND):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [sortBy:relevance]
then the source and the follow-up output(s) should have the same items in the same order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR355 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by removing the following parameters [feature, input, numberOfPlayers, playArea, vrSupport]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR406 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, feature, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature, language, specialOffers]
and a follow-up test case is constructed by removing the following parameters [os, term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR48 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, price, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [language]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR311 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, term]
and a follow-up test case is constructed by removing the following parameters [os, price, specialOffers]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR11 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, playArea, selectedTypes, specialOffers, tag]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR107 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [language, numberOfPlayers, vrSupport]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR61 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, language, numberOfPlayers, playArea, specialOffers, tag, term, vrSupport]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR286 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os, price, selectedTypes, specialOffers, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR200 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [price, selectedTypes]
and a follow-up test case is constructed by removing the following parameters [input, playArea, term]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR324 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, price, specialOffers, selectedTypes, numberOfPlayers, feature, headsets, os]
and a follow-up test case is constructed by adding the following parameters [input, language, playArea, tag, vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR64 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, headsets, input, playArea, language]
and a follow-up test case is constructed by removing the following parameters [language, numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, input, playArea, selectedTypes, specialOffers, tag, term, vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR155 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [feature, language, price, selectedTypes, specialOffers, term]
and a follow-up test case is constructed by removing the following parameters [tag]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR284 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by removing the following parameters [feature, numberOfPlayers, os, price, selectedTypes, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR238 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, input, language, term]
and a follow-up test case is constructed by removing the following parameters [tag]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR388 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, input, playArea]
and a follow-up test case is constructed by removing the following parameters [headsets, input, numberOfPlayers, playArea, price, selectedTypes, specialOffers, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR143 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [language, tag]
and a follow-up test case is constructed by adding the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR111 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by adding the following parameters [headsets, playArea, term]
and a follow-up test case is constructed by adding the following parameters [feature]
and a follow-up test case is constructed by adding the following parameters [input, specialOffers, tag]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR151 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, price, specialOffers, tag, selectedTypes, feature, language]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, tag, term]
and a follow-up test case is constructed by removing the following parameters [feature, language, price, specialOffers]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR298 (SHUFFLING_EXTEND):
if a source test case is run with the inputs [sortBy:lowestPrice, price, specialOffers, tag, selectedTypes, numberOfPlayers, feature, vrSupport, os]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:relevance]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:releaseDate]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:name]
and a follow-up test case is constructed by changing the value of the following parameters [sortBy:highestPrice]
then the source and the follow-up output(s) should have the same items, regardless of their order.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR126 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, specialOffers, numberOfPlayers, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [headsets, input, price, specialOffers]
and a follow-up test case is constructed by removing the following parameters [term]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers]
and a follow-up test case is constructed by removing the following parameters [os, playArea]
and a follow-up test case is constructed by removing the following parameters [feature]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR26 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, specialOffers, tag, selectedTypes, numberOfPlayers, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [input, numberOfPlayers, playArea, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR83 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [sortBy, price, tag, numberOfPlayers, feature]
and a follow-up test case is constructed by adding the following parameters [headsets, input, language, os, selectedTypes, term, vrSupport]
and a follow-up test case is constructed by adding the following parameters [playArea, specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR195 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, numberOfPlayers, vrSupport, headsets, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, input, language]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR4 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by removing the following parameters [feature, headsets, language, price, term]
and a follow-up test case is constructed by removing the following parameters [vrSupport]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR368 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, numberOfPlayers, feature, vrSupport, input, playArea]
and a follow-up test case is constructed by adding the following parameters [headsets, os]
and a follow-up test case is constructed by adding the following parameters [price, tag]
and a follow-up test case is constructed by adding the following parameters [specialOffers]
and a follow-up test case is constructed by adding the following parameters [language, selectedTypes]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR257 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [sortBy, price, specialOffers, tag, selectedTypes, feature, headsets, input, playArea, os]
and a follow-up test case is constructed by removing the following parameters [feature, input, playArea, price, specialOffers, tag]
and a follow-up test case is constructed by removing the following parameters [os]
and a follow-up test case is constructed by removing the following parameters [headsets, selectedTypes]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR222 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, tag, selectedTypes, input, playArea, os]
and a follow-up test case is constructed by adding the following parameters [feature, language, numberOfPlayers, specialOffers, vrSupport]
and a follow-up test case is constructed by adding the following parameters [headsets]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR235 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [term, sortBy, tag, numberOfPlayers, vrSupport, headsets, input, language]
and a follow-up test case is constructed by removing the following parameters [headsets, input, language, numberOfPlayers, tag, vrSupport]
and a follow-up test case is constructed by removing the following parameters [term]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR15 (CONJUNCTIVE_EXTEND):
if a source test case is run with the inputs [term, sortBy, price, feature, vrSupport, headsets, language]
and a follow-up test case is constructed by adding the following parameters [input, numberOfPlayers, playArea, specialOffers, tag]
and a follow-up test case is constructed by adding the following parameters [selectedTypes]
and a follow-up test case is constructed by adding the following parameters [os]
then the follow-up output(s) should be a (non-strict) subset of the source test output.

In the domain of Steam (SearchTheStore)
the following metamorphic relation should hold
MR103 (CONJUNCTIVE_CONTRACT):
if a source test case is run with the inputs [price, selectedTypes, numberOfPlayers, vrSupport, os, language]
and a follow-up test case is constructed by removing the following parameters [language]
and a follow-up test case is constructed by removing the following parameters [price]
and a follow-up test case is constructed by removing the following parameters [selectedTypes, vrSupport]
and a follow-up test case is constructed by removing the following parameters [numberOfPlayers, os]
then the follow-up output(s) should be a (non-strict) superset of the source test output.

